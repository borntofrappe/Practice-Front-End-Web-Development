import styled from 'styled-components';

/* styled component SVGIcon
component used for the SVG(s) included in both buttons
*/
const SVGIcon = styled.svg`
  width: 100%;
  height: 100%;
  fill: currentColor;
`;

export default SVGIcon;